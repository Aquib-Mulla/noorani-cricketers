<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<title>Player Details - <%= player.name %></title>

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

<style>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: "Poppins", sans-serif;
  }

  body {
    background: #1c1e21;
    color: #f5f5f5;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    padding: 20px;
    flex-direction: column;
  }

  /* Back Button (inline) */
  .back-btn {
    background: #e45305;
    color: #fff;
    padding: 10px 18px;
    border-radius: 25px;
    text-decoration: none;
    font-weight: 500;
    margin-bottom: 20px;
    transition: 0.3s;
  }
  .back-btn:hover {
    background: #c74303;
  }

  /* Container */
  .player-card {
    display: flex;
    background: #f5f5f5;
    border-radius: 20px;
    overflow: hidden;
    max-width: 900px;
    width: 100%;
    box-shadow: 0 5px 25px rgba(0,0,0,0.3);
  }

  /* Left Photo Panel */
  .player-photo {
    flex: 1;
    background: #1c1e21;
    display: flex;
    justify-content: center;
    align-items: center;
  }
  .player-photo img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: 0.4s;
  }
  .player-photo img:hover {
    transform: scale(1.05);
  }

  /* Right Info Panel */
  .player-info {
    flex: 1;
    padding: 40px 30px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
  }

  .player-info h1 {
    color: #e45305;
    font-size: 28px;
    margin-bottom: 5px;
  }
  .player-info h3 {
    color: #1c1e21;
    font-size: 18px;
    margin-bottom: 20px;
  }

  .details {
    display: flex;
    flex-direction: column;
    gap: 10px;
    margin-bottom: 20px;
  }
  .details p {
    font-size: 15px;
    color: #333;
    background: #fff;
    padding: 10px 15px;
    border-radius: 10px;
    box-shadow: 0 2px 6px rgba(0,0,0,0.1);
  }

  .bio {
    font-size: 15px;
    line-height: 1.7;
    color: #333;
    margin-bottom: 25px;
  }

  /* Social Links */
  .social-links {
    display: flex;
    gap: 15px;
  }
  .social-links a {
    text-decoration: none;
    color: #fff;
    background: #e45305;
    padding: 12px;
    border-radius: 50%;
    font-size: 18px;
    display: flex;
    justify-content: center;
    align-items: center;
    transition: 0.3s;
  }
  .social-links a:hover {
    background: #c74303;
    transform: scale(1.2);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .player-card {
      flex-direction: column;
    }
    .player-photo {
      height: 300px;
    }
    .player-info {
      padding: 25px;
    }
    .player-info h1 {
      font-size: 24px;
    }
    .player-info h3 {
      font-size: 16px;
    }
    .details p, .bio {
      font-size: 14px;
    }
    .social-links {
      justify-content: center;
    }
  }
</style>
</head>

<body>

<a href="/players" class="back-btn">
  <i class="fa-solid fa-arrow-left"></i> Back
</a>

<div class="player-card">

  <!-- PLAYER IMAGE -->
  <div class="player-photo">
    <img src="/uploads/<%= player.image %>" alt="<%= player.name %>">
  </div>

  <!-- PLAYER INFO -->
  <div class="player-info">

    <div>
      <h1><%= player.name %></h1>
      <h3>🏏 <%= player.role %> | Noorani Cricketers</h3>

      <div class="details">
        <p><strong>Age:</strong> <%= player.age %></p>
        <!-- <p><strong>Education:</strong> <%= player.education %></p> -->
        <p><strong>Mobile Number:</strong> <%= player.mobile %></p>
        <p><strong>Role:</strong> <%= player.role %></p>
      </div>

      <p class="bio">
        <%= player.description %>
      </p>
    </div>

    <!-- SOCIAL LINKS -->
    <div class="social-links">
      <% if (player.email) { %>
        <a href="mailto:<%= player.email %>">
          <i class="fa-solid fa-envelope"></i>
        </a>
      <% } %>

      <% if (player.mobile) { %>
        <a href="https://wa.me/<%= player.mobile %>" target="_blank">
          <i class="fa-brands fa-whatsapp"></i>
        </a>
      <% } %>

      <% if (player.instagram) { %>
        <a href="https://www.instagram.com/<%= player.instagram %>" target="_blank">
          <i class="fa-brands fa-instagram"></i>
        </a>
      <% } %>
    </div>

  </div>
</div>

</body>
</html>
